<!-- FLOATING WHATSAPP BUTTON (BOTTOM RIGHT - COMPACT) -->
<a
  id="FloatingWhatsAppBtn"
  class="floating-whatsapp-btn"
  href="https://wa.me/919723328111"
  target="_blank"
  rel="noopener noreferrer"
  aria-label="Chat on WhatsApp"
  title="Chat on WhatsApp"
>
  <span class="floating-whatsapp-pulse"></span>
  <svg viewBox="0 0 32 32" class="floating-whatsapp-icon" aria-hidden="true" focusable="false">
    <path fill="currentColor" d="M16 2a13.9 13.9 0 0 0-12 21L2 30l7.2-1.9A14 14 0 1 0 16 2zm0 25.6a11.6 11.6 0 0 1-5.9-1.6l-.4-.3-4.4 1.1 1.2-4.2-.3-.5A11.6 11.6 0 1 1 16 27.6zm6.4-8.7c-.3-.2-2-1-2.3-1.1-.3-.1-.6-.2-.8.2s-.9 1.1-1.1 1.3-.4.2-.7.1a9.2 9.2 0 0 1-4.2-3.7c-.3-.6.3-.6.9-1.7.1-.2 0-.4 0-.5s-.8-2-1.1-2.7c-.3-.7-.6-.6-.8-.6h-.7a1.4 1.4 0 0 0-1 .5 4.3 4.3 0 0 0-1.3 3.2 7.6 7.6 0 0 0 1.6 4 17.5 17.5 0 0 0 6.7 5.9c2.4 1 3.3.8 3.9.8a3.3 3.3 0 0 0 2.2-1.5 2.7 2.7 0 0 0 .2-1.5c-.1-.3-.3-.4-.6-.6z"/>
  </svg>
  <span class="floating-whatsapp-tooltip">Chat with us</span>
</a>

<style>
  .floating-whatsapp-btn {
    position: fixed;
    bottom: 22px;
    right: 22px;
    width: 44px;
    height: 44px;
    background: #25D366;
    color: #ffffff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 6px 20px rgba(37, 211, 102, 0.4), 0 2px 6px rgba(0, 0, 0, 0.15);
    z-index: 99998;
    text-decoration: none;
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 0.3s ease,
                background-color 0.25s ease;
  }

  .floating-whatsapp-icon {
    width: 24px;
    height: 24px;
    z-index: 2;
    transition: transform 0.3s ease;
  }

  /* Soft Ambient Pulse */
  .floating-whatsapp-pulse {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: #25D366;
    opacity: 0.6;
    z-index: 1;
    animation: waPulse 2.4s cubic-bezier(0.24, 0, 0.38, 1) infinite;
    pointer-events: none;
  }

  @keyframes waPulse {
    0% {
      transform: scale(1);
      opacity: 0.6;
    }
    70% {
      transform: scale(1.4);
      opacity: 0;
    }
    100% {
      transform: scale(1.4);
      opacity: 0;
    }
  }

  /* Hover & Active States */
  .floating-whatsapp-btn:hover {
    background: #20ba59;
    color: #ffffff;
    transform: translateY(-3px) scale(1.08);
    box-shadow: 0 10px 26px rgba(37, 211, 102, 0.55), 0 3px 8px rgba(0, 0, 0, 0.2);
  }

  .floating-whatsapp-btn:hover .floating-whatsapp-icon {
    transform: scale(1.06);
  }

  .floating-whatsapp-btn:active {
    transform: translateY(0) scale(0.96);
  }

  /* Tooltip on Desktop Hover (Positioned to the left of button) */
  .floating-whatsapp-tooltip {
    position: absolute;
    right: calc(100% + 10px);
    top: 50%;
    transform: translateY(-50%) translateX(6px);
    background: #111111;
    color: #ffffff;
    font-size: 1.2rem;
    font-weight: 500;
    padding: 5px 12px;
    border-radius: 16px;
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 0.25s ease, transform 0.25s ease, visibility 0.25s ease;
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.2);
    font-family: inherit;
  }

  .floating-whatsapp-tooltip::before {
    content: '';
    position: absolute;
    left: 100%;
    top: 50%;
    transform: translateY(-50%);
    border-width: 5px;
    border-style: solid;
    border-color: transparent transparent transparent #111111;
  }

  @media (hover: hover) {
    .floating-whatsapp-btn:hover .floating-whatsapp-tooltip {
      opacity: 1;
      visibility: visible;
      transform: translateY(-50%) translateX(0);
    }
  }

  @media (max-width: 768px) {
    .floating-whatsapp-btn {
      bottom: 16px;
      right: 16px;
      width: 40px;
      height: 40px;
    }
    .floating-whatsapp-icon {
      width: 22px;
      height: 22px;
    }
    .floating-whatsapp-tooltip {
      display: none;
    }
  }
</style>
